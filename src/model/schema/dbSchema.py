schema = {
    "type": "object",
    "properties": {
        "discrete_id": {"type": "string"},
        "version": {"type": "string"},
        "task_id": {"type": "string"},
        "metadata": {"type": "object"} # tiffs array stored inside this object
    }
}
